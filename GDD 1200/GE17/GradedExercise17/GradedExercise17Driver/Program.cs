using GradedExercise17;

namespace GradedExercise17Driver
{
    /// <summary>
    /// Graded Exercise 17 driver
    /// </summary>
    internal class Program
    {
        /// <summary>
        /// Tests the Apple properties
        /// </summary>
        /// <param name="args">command-line args</param>
        static void Main(string[] args)
        {
            // In C#, we actually get a default, no parameter
            // constructor if we don't implement our own constructor
            Apple apple = new Apple();

            // STUDENTS: Uncomment the code below after you've
            // implemented the Apple fields and properties
            //Console.WriteLine("Organic? " + apple.Organic);
            //Console.WriteLine("Amount Left: " + apple.AmountLeft);
        }
    }
}